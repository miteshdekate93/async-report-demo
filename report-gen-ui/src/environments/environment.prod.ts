// Production environment — injected by angular.json fileReplacements during production builds.
// Update these URLs to point at your deployed API before running `ng build --configuration production`.
export const environment = {
  production: true,
  apiUrl: 'https://your-api.example.com',
  hubUrl: 'https://your-api.example.com/hubs/report',
};
