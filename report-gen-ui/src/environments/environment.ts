// Development environment — used when running `ng serve` (the default configuration).
// angular.json replaces this file with environment.prod.ts during `ng build --configuration production`.
export const environment = {
  production: false,
  apiUrl: 'http://localhost:5000',
  hubUrl: 'http://localhost:5000/hubs/report',
};
